"use strict";
const sum = (x, y) => {
    return x + y;
};
console.log(sum(10, 10));
// void -> função sem retorno
const showMessage = (message) => {
    console.log(message);
};
showMessage('Olá, Mundo!');
